
let lastWords = "...";
let wordBrightness = 255;
let yOffset = 0;

function draw_one_frame(words, vocal, drum, bass, other, counter) {
background(0);
fill(255)
let backgroundColour = map(counter, 0, song.duration(), 0, 255)
//console.log(song.duration())

// demonstrate use of non-documented "counter" variable
let seconds = counter
console.log(counter)
  textSize(60);
  text(seconds, 20, height-90);


}
